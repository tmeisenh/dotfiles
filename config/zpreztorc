#
# Sets Prezto options.
# Author: Travis B. Meisenheimer
#

# highlight autosuggestions
zstyle ':prezto:module:autosuggestions' color 'yes'

zstyle ':prezto:module:syntax-highlighting' highlighters \
  'main' \
  'brackets' \
  'pattern' \
  'cursor' \
  'root'

# Set the key mapping style to 'emacs' or 'vi'.
zstyle ':prezto:module:editor' keymap 'vi'
zstyle ':prezto:module:editor' dot-expansion 'yes'

# use my git aliases
zstyle ':prezto:module:git:alias' skip 'yes'

# Set case-sensitivity for completion, history lookup, etc.
zstyle ':prezto:*:*' case-sensitive 'no'

zstyle ':prezto:module:history-substring-search' case-sensitive 'no'
zstyle ':prezto:module:history-substring-search' color 'yes'

# Color output (auto set to 'no' on dumb terminals).
zstyle ':prezto:*:*' color 'yes'

# Auto set the tab and window titles.
zstyle ':prezto:module:terminal' auto-title 'yes'

# Set the Zsh modules to load (man zshmodules).
# zstyle ':prezto:load' zmodule 'attr' 'stat'

# Set the Zsh functions to load (man zshcontrib).
zstyle ':prezto:load' zfunction 'zargs' 'zmv'

# ssh
zstyle ':prezto:module:ssh:load' identities 'id_rsa' 'id_ecdsa' 

# Set the Prezto modules to load (browse modules).
# The order matters.
zstyle ':prezto:load' pmodule \
  'environment' \
  'terminal' \
  'editor' \
  'history' \
  'history-substring-search' \
  'directory' \
  'spectrum' \
  'gnu-utility' \
  'utility' \
  'completion' \
  'git' \
  'archive' \
  'osx' \
  'perl' \
  'python' \
  'ruby' \
  'ssh' \
  'node' \
  'docker' \
  'syntax-highlighting' \
  'history-substring-search' \
  'prompt'

# Set the prompt theme to load.
# Setting it to 'random' loads a random theme.
# Auto set to 'off' on dumb terminals.
zstyle ':prezto:module:prompt' theme 'tmeisenh'

